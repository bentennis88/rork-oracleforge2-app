/**
 * Convenience re-export to match common naming.
 * The actual store lives in `oracles/OracleStore.tsx`.
 */
export { OraclesProvider, useOracleStore } from '@/oracles/OracleStore';
export type { Oracle, OracleStore, OracleType } from '@/oracles/OracleStore';

