// Re-export types from OraclesContext for backward compatibility with skeleton components
export type {
  TrackerOracleConfig,
  ReminderOracleConfig,
  CalculatorOracleConfig,
  OracleConfig,
} from '@/contexts/OraclesContext';
